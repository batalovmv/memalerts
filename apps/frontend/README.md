# MemAlerts Frontend

React приложения для Channel Points Mem Alerts - системы активации мемов через Twitch Channel Points.

## 🚀 Функционал

### Web приложение
- **Аутентификация** - Вход через Twitch OAuth
- **Профили каналов** - Публичные профили стримеров с каталогом мемов
- **Управление балансом** - Отображение баланса монет в header, real-time обновление через Socket.IO
- **Активация мемов** - Просмотр и активация мемов за монеты
- **Подача заявок** - Загрузка новых мемов для модерации
- **Админ панель** - Управление заявками, мемами и настройками канала
- **Поиск** - Поиск мемов по названию на странице профиля
- **Локализация** - Полная поддержка русского и английского языков
- **Twitch интеграция** - Отображение аватарок и кастомных иконок монет из Twitch

## 🛣️ Префиксы API роутов (чтобы не путаться)

- **`/streamer/*`**: панель стримера (роль `streamer` или `admin`)
- **`/owner/*`**: owner-only операции (роль `admin`)
- **`/admin/*`**: legacy алиас (обратная совместимость)

### Overlay приложение
- Real-time отображение активированных мемов в OBS
- Поддержка изображений, GIF, видео и аудио
- Настройка позиции и масштаба через URL параметры
- Автоматическое управление воспроизведением

## 📋 Страницы

- `/` - Лендинг с кнопкой входа через Twitch
- `/dashboard` - Главная страница пользователя
- `/channel/:slug` - Публичный профиль канала
  - Каталог мемов
  - Поиск мемов
  - Кнопка подачи заявки (для не-владельцев)
- `/settings` - Админ панель (только для streamer/admin)
  - Очередь заявок на модерацию
  - Управление мемами
  - Настройки канала (Twitch Rewards, цвета, профиль)
- `/submit` - Подача заявки на новый мем (устарело, используется модальное окно)
- `/search` - Глобальный поиск мемов

## 🏗️ Структура проекта

```
.
├── src/                    # Web приложение
│   ├── pages/             # Страницы
│   ├── components/         # React компоненты
│   ├── store/              # Redux store
│   ├── lib/                # Утилиты (API клиент)
│   ├── locales/            # Переводы (i18n)
│   └── types/              # TypeScript типы
├── overlay/                # Overlay приложение
│   ├── src/
│   │   └── OverlayView.tsx # Основной компонент overlay
│   └── ...
├── package.json
└── pnpm-workspace.yaml
```

## 🚀 Быстрый старт

### Требования
- Node.js >= 18.0.0
- pnpm >= 8.0.0

### Установка

```bash
pnpm install
```

### Настройка

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:3001
```

Создайте файл `.env` в директории `overlay/`:

```env
VITE_API_URL=http://localhost:3001
```

### Запуск

```bash
# Запустить все приложения одновременно
pnpm dev

# Или запустить отдельно:
pnpm dev:web      # Только Web приложение (http://localhost:5173)
pnpm dev:overlay  # Только Overlay (http://localhost:5174)
```

## 📦 Команды

```bash
# Разработка
pnpm dev              # Запустить все приложения
pnpm dev:web          # Запустить только Web
pnpm dev:overlay      # Запустить только Overlay

# Сборка
pnpm build            # Собрать все приложения
pnpm build:web        # Собрать только Web
pnpm build:overlay    # Собрать только Overlay

# Линтинг
pnpm lint             # Проверить код всех приложений
```

## 🎨 Технологии

- React 18
- TypeScript
- Vite
- Tailwind CSS
- React Router
- Redux Toolkit
- Axios
- Socket.IO Client
- React i18next
- React Hot Toast

## 🎬 Overlay для OBS

### Использование

1. Добавьте Browser Source в OBS
2. Укажите URL:
   ```
   http://localhost:5174/overlay/YOUR_CHANNEL_SLUG?scale=1&position=center&volume=1
   ```
3. Настройте параметры:
   - `scale` - Масштаб (0.5-2.0)
   - `position` - Позиция (center, top, bottom, top-left, top-right, bottom-left, bottom-right)
   - `volume` - Громкость для аудио/видео (0.0-1.0)
4. Установите размер: 1920x1080 (или ваше разрешение стрима)

### Поддерживаемые форматы
- Изображения (JPEG, PNG, WebP)
- GIF анимации
- Видео (MP4, WebM)
- Аудио (MP3, WAV, OGG)

## 🔌 API интеграция

Frontend приложения подключаются к Backend API через:
- REST API для получения данных и выполнения действий
- Socket.IO для real-time обновлений (баланс, активации мемов)

URL API настраивается через переменную окружения `VITE_API_URL`.

## 🚢 Деплой

### Production сборка

```bash
pnpm build
```

Собранные файлы находятся в:
- `dist/` - Web приложение
- `overlay/dist/` - Overlay приложение

### Настройка Nginx

Пример конфигурации для Web:

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    root /path/to/memalerts-frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## 🔮 Планы на будущее

### Ближайшие планы
- [ ] Глобальная страница всех мемов проекта
- [ ] Улучшенный поиск с фильтрами
- [ ] Темная/светлая тема
- [ ] Мобильная адаптация

### Долгосрочные планы
- [ ] Мобильное приложение
- [ ] PWA поддержка
- [ ] Офлайн режим
- [ ] Push уведомления

## 🐛 Troubleshooting

### Приложение не подключается к API
- Проверьте `VITE_API_URL` в `.env` файлах
- Убедитесь, что Backend API запущен и доступен
- Проверьте CORS настройки на Backend

### Overlay не показывает мемы
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что Socket.IO подключен
- Проверьте, что `channelSlug` в URL совпадает с slug канала в БД

### Ошибки сборки
- Убедитесь, что все зависимости установлены: `pnpm install`
- Проверьте версию Node.js: должна быть >= 18.0.0
- Очистите кэш и пересоберите: `rm -rf node_modules dist && pnpm install && pnpm build`

## 📄 Лицензия

MIT
