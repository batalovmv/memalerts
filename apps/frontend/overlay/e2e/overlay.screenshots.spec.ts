import { describe, it } from 'vitest';

// Placeholder suite.
// The screenshot e2e is run by Playwright, not Vitest.
// Keeping a skipped test here prevents Vitest from erroring with "No test suite found".
describe.skip('overlay screenshots (playwright)', () => {
  it('is covered by Playwright E2E, not Vitest', () => {
    // no-op
  });
});
