# MemAlerts Backend

Backend API для системы активации мемов через Twitch Channel Points. Express API с Socket.IO, PostgreSQL и автоматическим деплоем через GitHub Actions.

## 🚀 Функционал

### Аутентификация
- Twitch OAuth 2.0 авторизация
- Автоматическое создание каналов для стримеров
- JWT токены в httpOnly cookies
- Роли: viewer, streamer, admin
- Сохранение Twitch токенов для API запросов
- Автоматическое обновление Twitch токенов

## 🛣️ Префиксы API роутов (важно для понимания прав)

Исторически часть панели стримера жила под `/admin/*`. Чтобы не путаться:

- **`/streamer/*`**: панель стримера (доступ: `streamer` или `admin`)
- **`/owner/*`**: owner-only операции (доступ: только `admin`)
- **`/admin/*`**: legacy алиас для обратной совместимости (новый код должен использовать `/streamer` и `/owner`)

### Управление мемами
- Загрузка мемов пользователями (заявки на одобрение)
- Модерация заявок стримерами (одобрение/отклонение)
- Активация мемов зрителями за виртуальные монеты
- Поддержка типов: image, gif, video, audio
- Бесплатные мемы для владельцев канала
- Система тегов для мемов
- Поиск мемов по названию и тегам

### Система баланса
- Виртуальные монеты для каждого пользователя и канала
- Интеграция с Twitch Channel Points Rewards
- Автоматическое получение монет через EventSub webhooks
- Трата монет на активацию мемов
- Настройка конвертации Channel Points → монеты
- Real-time обновление баланса через Socket.IO

### Интеграция с Twitch
- Создание и управление Channel Points Rewards через Twitch API
- Автоматическая подписка на EventSub для отслеживания наград
- Получение кастомных иконок монет из Twitch
- Сохранение Twitch аватарок пользователей

### Профили стримеров
- Уникальные профили по slug канала
- Публичный API для получения информации о канале
- Статистика канала (количество мемов, пользователей)
- Настройки канала (цвета, награды, иконки)

### Real-time коммуникация
- Socket.IO для overlay в OBS
- Мгновенная отправка активированных мемов на overlay
- Real-time обновление баланса кошелька
- Поддержка множественных каналов

### Безопасность
- **Аутентификация и авторизация**
  - JWT токены в httpOnly cookies (изоляция beta/production через разные `JWT_SECRET`)
  - Проверка прав доступа к каналам (пользователи могут работать только со своими каналами)
  - Beta access control middleware
- **Защита от атак**
  - CSRF защита (проверка Origin/Referer для state-changing операций)
  - Path traversal protection (санитизация имен файлов и валидация путей)
  - SQL injection protection (все запросы через Prisma)
  - XSS protection (Content Security Policy через Helmet)
  - File upload security (magic bytes validation, size limits, sanitization)
- **Rate limiting**
  - Глобальный: 100 запросов за 15 минут
  - Активация мемов: 1 запрос за 3 секунды
  - Загрузка файлов: 10 запросов за 1 минуту
  - IP whitelist support
- **Валидация данных**
  - Zod schemas на уровне приложения
  - CHECK constraints на уровне базы данных
  - Валидация переменных окружения при старте
- **Аудит и логирование**
  - Структурированное логирование критических действий
  - Аудит-логи в базе данных
  - Логирование событий безопасности
- **CORS**: Ограничен конкретными доменами (beta и production)
- **Webhook security**: HMAC проверка EventSub webhooks

Архитектура и структура модулей: см. [ARCHITECTURE.md](./ARCHITECTURE.md)

### Автоматический деплой
- CI/CD через GitHub Actions
- Автоматическое создание .env из GitHub Secrets
- Автоматическое применение миграций БД
- Деплой на VPS с PM2
- Поддержка Cloudflare Origin Certificate

## 📋 API Endpoints

### Публичные
- `GET /health` - Проверка работоспособности API
- `GET /channels/:slug` - Получить информацию о канале по slug
- `GET /channels/memes/search` - Поиск мемов по каналу

### Аутентификация
- `GET /auth/twitch` - Инициация Twitch OAuth
- `GET /auth/twitch/callback` - Callback от Twitch OAuth
- `POST /auth/logout` - Выход из системы

### Пользователь (требует авторизации)
- `GET /me` - Информация о текущем пользователе
- `GET /channels/:slug/wallet` - Баланс кошелька для канала
- `GET /channels/:slug/memes` - Список мемов канала
- `POST /memes/:id/activate` - Активировать мем

### Заявки на мемы (требует авторизации)
- `POST /submissions` - Создать заявку на мем (с загрузкой файла)
- `GET /submissions/mine` - Получить мои заявки

### Админ панель (требует роль streamer/admin)

> Исторически эти эндпоинты жили под `/admin/*`. Сейчас правильные префиксы:
> - `/streamer/*` (стример/админ)
> - `/owner/*` (только admin)
> `/admin/*` остаётся алиасом для обратной совместимости.

- `GET /streamer/submissions` - Получить заявки (с фильтром по статусу)
- `POST /streamer/submissions/:id/approve` - Одобрить заявку
- `POST /streamer/submissions/:id/reject` - Отклонить заявку
- `GET /streamer/memes` - Получить все мемы канала
- `PATCH /streamer/memes/:id` - Обновить мем (цена, длительность)
- `PATCH /streamer/channel/settings` - Обновить настройки канала
- `GET /streamer/stats/channel` - Статистика канала

### Webhooks
- `POST /webhooks/twitch/eventsub` - Webhook от Twitch EventSub для обработки Channel Points redemptions

## 🏗️ Структура проекта

```
.
├── prisma/
│   ├── schema.prisma    # Prisma схема базы данных
│   └── migrations/       # Миграции БД
├── src/
│   ├── controllers/       # Контроллеры (фасады + модульные папки)
│   │   ├── adminController.ts        # фасад -> controllers/admin/*
│   │   ├── viewerController.ts       # фасад -> controllers/viewer/*
│   │   ├── submissionController.ts   # фасад -> controllers/submission/*
│   │   ├── admin/                    # стример/owner эндпоинты (по фичам)
│   │   ├── viewer/                   # viewer эндпоинты (по фичам)
│   │   └── submission/               # submissions/upload/import (по фичам)
│   ├── middleware/      # Express middleware
│   │   ├── auth.ts
│   │   ├── errorHandler.ts
│   │   ├── rateLimit.ts
│   │   ├── upload.ts
│   │   └── requireChannel.ts
│   ├── routes/          # Маршруты API
│   ├── socket/          # Socket.IO настройка
│   ├── utils/           # Утилиты (Twitch API, валидация)
│   ├── lib/             # Утилиты (Prisma)
│   ├── shared/          # Общие типы и Zod схемы
│   └── index.ts         # Точка входа приложения
├── .github/
│   └── workflows/
│       └── ci-cd.yml    # GitHub Actions CI/CD
├── pnpm-workspace.yaml  # изоляция repo (pnpm работает внутри memalerts-backend)
├── pnpm-lock.yaml       # lockfile для воспроизводимых сборок
└── uploads/             # Загруженные файлы (мемы)
```

## 🚀 Быстрый старт

### Требования
- Node.js >= 18.0.0
- pnpm >= 8.0.0
- PostgreSQL >= 15

### Локальная разработка

```bash
# Установить зависимости
pnpm install

# Создать .env файл (см. раздел "Переменные окружения")
cp .env.example .env

# Настроить базу данных
pnpm prisma migrate dev

# Запустить в dev режиме
pnpm dev
```

### Production деплой

Проект настроен на автоматический деплой через GitHub Actions. При push в ветку `main`:
1. Запускаются тесты
2. Собирается проект
3. Автоматически создается `.env` на сервере из GitHub Secrets
4. Код деплоится на VPS
5. Применяются миграции БД
6. Приложение перезапускается через PM2

**Как запустить деплой прямо сейчас**:
- Production: сделай commit → push в `main` (или нажми `Run workflow` в Actions для `Backend CI/CD`).
- Beta: commit → push в `develop`.

## 📝 Переменные окружения

### Обязательные GitHub Secrets
- `VPS_HOST` - IP адрес сервера
- `VPS_USER` - Пользователь для SSH
- `VPS_SSH_KEY` - Приватный SSH ключ
- `VPS_PORT` - Порт SSH (обычно `22`)
- `DATABASE_URL` - Строка подключения к PostgreSQL
- `JWT_SECRET` - Секретный ключ для JWT токенов (для production)
- `TWITCH_CLIENT_ID` - Client ID из Twitch приложения
- `TWITCH_CLIENT_SECRET` - Client Secret из Twitch приложения
- `TWITCH_EVENTSUB_SECRET` - Секрет для проверки EventSub webhooks
- `WEB_URL` - URL фронтенда
- `OVERLAY_URL` - URL overlay

### Опциональные
- `DOMAIN` - Доменное имя
- `TWITCH_CALLBACK_URL` - Явный URL для callback
- `CLOUDFLARE_ORIGIN_CERT` - Cloudflare Origin Certificate
- `CLOUDFLARE_ORIGIN_KEY` - Cloudflare Origin Private Key
- `JWT_SECRET_BETA` - Секретный ключ для JWT токенов (для beta окружения). Если не установлен, используется `JWT_SECRET`. **Рекомендуется использовать отдельный секрет для изоляции токенов между бета и продакшен.**

## 📦 Команды

```bash
# Разработка
pnpm dev              # Запуск в dev режиме с hot reload

# Сборка
pnpm build            # Сборка TypeScript в JavaScript

# База данных
pnpm prisma migrate dev    # Создать и применить миграцию
pnpm prisma migrate deploy # Применить миграции в production
pnpm prisma generate      # Сгенерировать Prisma Client
pnpm prisma studio        # Открыть Prisma Studio (GUI для БД)

# Production
pnpm start            # Запуск собранного приложения
```

## 🔧 Настройка Twitch

1. Создайте приложение на https://dev.twitch.tv/console/apps
2. Получите Client ID и Client Secret
3. Добавьте Redirect URL:
   - Для локальной разработки: `http://localhost:3001/auth/twitch/callback`
   - Для production: `https://twitchmemes.ru/auth/twitch/callback`
4. Настройте EventSub подписку для `channel.channel_points_custom_reward_redemption.add`
5. Добавьте полученные данные в GitHub Secrets

## 🔮 Планы на будущее

### Функциональные улучшения
- [ ] Глобальная страница всех мемов проекта с поиском
- [ ] Расширенная статистика и аналитика
- [ ] Система промокодов и бонусов (частично реализовано через Promotion)
- [ ] Интеграция платежных систем для пополнения баланса
- [ ] Множественные каналы для пользователя
- [ ] Социальные функции (комментарии, рейтинги, избранные)
- [ ] Мобильное приложение (React Native)
- [ ] Публичный API для разработчиков
- [ ] Система монетизации

### Архитектурные улучшения и технический долг

- [ ] **Отдельная база данных для beta окружения**
  - **Текущее состояние**: Beta и production используют одну БД (`DATABASE_URL`)
  - **Риск**: Тестовые данные могут попасть в продакшен, изменения схемы на бете могут сломать продакшен
  - **Решение**: Рассмотреть отдельную БД для беты или отдельную схему PostgreSQL
  - **Приоритет**: Средний (архитектурное решение)

- [ ] **Разделение статических файлов для beta и production**
  - **Текущее состояние**: Beta и production используют одну директорию `/opt/memalerts-backend/uploads/`
  - **Риск**: Низкий, но тестовые файлы могут попасть в продакшен
  - **Решение**: Отдельная директория для беты или префиксы в именах файлов
  - **Приоритет**: Низкий

- [ ] **Улучшение контроля доступа к beta окружению**
  - **Текущее состояние**: Автоматическое предоставление beta access при логине на бете
  - **Риск**: Любой пользователь может получить beta access
  - **Решение**: Более строгий контроль доступа или явное одобрение администратором
  - **Приоритет**: Средний

- [ ] **Система мониторинга и алертов**
  - **Текущее состояние**: Логирование в консоль (PM2 logs) и базу данных
  - **Риск**: Проблемы могут остаться незамеченными до серьезных инцидентов
  - **Решение**: Интеграция с Sentry (отслеживание ошибок), DataDog/Grafana (метрики), алерты на подозрительную активность
  - **Приоритет**: Средний (рекомендация для будущего)

- [ ] **Улучшение обработки ошибок**
  - **Текущее состояние**: Базовая обработка ошибок через errorHandler middleware
  - **Решение**: Более детальная обработка различных типов ошибок, улучшенные сообщения для пользователей
  - **Приоритет**: Низкий

## 🐛 Troubleshooting

### Ошибка подключения к базе данных
- Убедитесь, что PostgreSQL запущен
- Проверьте правильность `DATABASE_URL`
- Убедитесь, что база данных создана

### EventSub не работает
- Убедитесь, что webhook URL публично доступен
- Проверьте, что `TWITCH_EVENTSUB_SECRET` совпадает с секретом в подписке
- Проверьте логи: `pm2 logs memalerts-api`

### Файлы не загружаются
- Убедитесь, что директория `uploads/` существует
- Проверьте права доступа к директории
- Проверьте `MAX_FILE_SIZE` в `.env`

## 📄 Лицензия

MIT
